# YOLOv8-Pose 数据集配置文件
# 适用于21个关键点的手部姿态估计

# -----------------------------------------------------------------
# 🚀 路径配置 (您必须修改这些)
# -----------------------------------------------------------------
# path: "C:/Users/YourName/Datasets/MyHandDataset"  # Windows 示例
path: "/home/user/datasets/my_hand_dataset"         # Linux 示例 (数据集的根目录)

# 训练集和验证集的相对路径 (相对于上面的 'path')
train: images/train
val: images/val
# test: images/test  # (可选) 如果您有单独的测试集

# -----------------------------------------------------------------
# 🎯 关键点配置 (这是您任务的核心)
# -----------------------------------------------------------------
kpt_shape: [21, 3]  # [关键点数量, 维度]
                    # 21 个手部关键点
                    # 3 维 (x, y, visibility)

# -----------------------------------------------------------------
# 🏷️ 类别配置
# -----------------------------------------------------------------
nc: 1  # 类别数量 (我们只检测一个类别："手")
names:
  0: hand  # 类别 0 的名称
